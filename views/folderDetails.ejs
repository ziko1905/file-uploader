<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Folder Details</title>
</head>
<body>
    <%- include("./partials/navbar") %>
    <h1><%= locals.folder.name %></h1>
    <h3><%= locals.folder.creationDate %></h3>
    <h2>Update:</h2>
    <form action="/folder/<%= locals.folder.id %>/details" method="POST">
        <label for="name">Folder name:</label>
        <input value="<%= locals.folder.name %>" type="text" name="name" id="name">
        <button type="submit">Update</button>
    </form>
    <form action="/folder/<%= locals.folder.id %>/delete" method="GET">
        <button type="submit" onclick="return confirm('Are you sure you want to delete the folder. (with all files in it)?')">Delete</button>
    </form>
</body>
</html>